<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>정보제공 동의 - 법무법인 로연 x 서울대학교</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.4.0/css/all.min.css">
    <link rel="stylesheet" href="css/ui-components.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Noto Sans KR', sans-serif;
            /* 서울대학교 메인 컬러: SNU Blue */
            background: linear-gradient(135deg, #1D2088 0%, #151A66 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        .back-btn {
            background: rgba(255,255,255,0.2);
            border: 2px solid white;
            color: white;
            padding: 10px 20px;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            margin-bottom: 20px;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
        }

        .back-btn:hover {
            background: white;
            color: #1D2088;
        }

        .consent-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            animation: fadeInUp 0.6s ease;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .consent-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .consent-header h1 {
            font-size: 26px;
            color: #2c3e50;
            margin-bottom: 10px;
        }

        .consent-header p {
            color: #6b7280;
            font-size: 15px;
        }

        .service-info {
            background: #E8EAFF;
            border: 2px solid #1D2088;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 30px;
        }

        .service-info h3 {
            font-size: 18px;
            color: #1D2088;
            margin-bottom: 12px;
        }

        .service-info-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
        }

        .info-item {
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .info-label {
            font-size: 12px;
            color: #6b7280;
        }

        .info-value {
            font-size: 15px;
            font-weight: 600;
            color: #2c3e50;
        }

        .consent-section {
            margin-bottom: 30px;
        }

        .consent-section h3 {
            font-size: 18px;
            color: #2c3e50;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .consent-box {
            background: #f9fafb;
            border: 2px solid #e5e7eb;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 15px;
            max-height: 200px;
            overflow-y: auto;
        }

        .consent-box h4 {
            font-size: 15px;
            color: #374151;
            margin-bottom: 10px;
        }

        .consent-box p {
            font-size: 13px;
            color: #6b7280;
            line-height: 1.8;
            margin-bottom: 10px;
        }

        .consent-box ul {
            list-style: none;
            margin-left: 15px;
        }

        .consent-box ul li {
            font-size: 13px;
            color: #6b7280;
            line-height: 1.8;
            position: relative;
            padding-left: 15px;
        }

        .consent-box ul li::before {
            content: '•';
            position: absolute;
            left: 0;
            color: #1D2088;
        }

        .consent-checkbox {
            display: flex;
            align-items: start;
            gap: 12px;
            padding: 15px;
            background: white;
            border: 2px solid #e5e7eb;
            border-radius: 10px;
            margin-bottom: 15px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .consent-checkbox:hover {
            border-color: #1D2088;
            background: #f9fafb;
        }

        .consent-checkbox.checked {
            border-color: #1D2088;
            background: #f0f4ff;
        }

        .consent-checkbox input[type="checkbox"] {
            width: 20px;
            height: 20px;
            cursor: pointer;
            accent-color: #1D2088;
        }

        .consent-label {
            flex: 1;
            cursor: pointer;
        }

        .consent-label .title {
            font-size: 15px;
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 5px;
        }

        .consent-label .required {
            color: #ef4444;
            font-weight: 700;
            margin-left: 5px;
        }

        .consent-label .optional {
            color: #10b981;
            font-weight: 700;
            margin-left: 5px;
        }

        .consent-label .desc {
            font-size: 13px;
            color: #6b7280;
            line-height: 1.6;
        }

        .view-detail {
            font-size: 12px;
            color: #1D2088;
            text-decoration: underline;
            cursor: pointer;
            margin-left: 5px;
        }

        .button-group {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }

        .btn {
            flex: 1;
            padding: 16px;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-cancel {
            background: #f3f4f6;
            color: #374151;
        }

        .btn-cancel:hover {
            background: #e5e7eb;
        }

        .btn-submit {
            background: linear-gradient(135deg, #1D2088 0%, #151A66 100%);
            color: white;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 78, 161, 0.4);
        }

        .btn-submit:disabled {
            background: #e5e7eb;
            color: #9ca3af;
            cursor: not-allowed;
            transform: none;
        }

        @media (max-width: 768px) {
            .consent-container {
                padding: 30px 20px;
            }

            .service-info-grid {
                grid-template-columns: 1fr;
            }

            .button-group {
                flex-direction: column-reverse;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <button class="back-btn" onclick="history.back()">
            <i class="fas fa-arrow-left"></i> 돌아가기
        </button>

        <div class="consent-container">
            <div class="consent-header">
                <h1>정보제공 동의</h1>
                <p>서비스 신청 전 아래 내용을 확인하고 동의해주세요</p>
            </div>

            <div class="service-info">
                <h3><i class="fas fa-file-alt"></i> 신청하실 서비스</h3>
                <div class="service-info-grid">
                    <div class="info-item">
                        <div class="info-label">서비스명</div>
                        <div class="info-value" id="serviceName">-</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">서비스 금액</div>
                        <div class="info-value" id="serviceAmount">-</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">신청자</div>
                        <div class="info-value" id="applicantName">-</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">소속</div>
                        <div class="info-value" id="university">전남대학교</div>
                    </div>
                </div>
            </div>

            <form id="consentForm">
                <div class="consent-section">
                    <h3><i class="fas fa-shield-alt"></i> 필수 동의 항목</h3>
                    
                    <div class="consent-checkbox" onclick="toggleCheckbox('consent1')">
                        <input type="checkbox" id="consent1" required>
                        <div class="consent-label">
                            <div class="title">
                                개인정보 수집 및 이용 동의
                                <span class="required">(필수)</span>
                                <span class="view-detail" onclick="event.stopPropagation(); viewDetail('consent1-detail')">자세히 보기</span>
                            </div>
                            <div class="desc">성명, 국적, 여권번호, 체류자격 등 개인정보 수집 및 이용에 동의합니다.</div>
                        </div>
                    </div>

                    <div class="consent-box" id="consent1-detail" style="display: none;">
                        <h4>개인정보 수집 및 이용 동의</h4>
                        <p><strong>1. 수집하는 개인정보 항목</strong></p>
                        <ul>
                            <li>필수항목: 성명, 국적, 여권번호, 체류자격, 외국인등록번호, 체류기간</li>
                            <li>선택항목: 학번, 전공, 연락처, 이메일 주소</li>
                        </ul>
                        <p><strong>2. 개인정보의 수집 및 이용 목적</strong></p>
                        <ul>
                            <li>출입국 민원 대행 서비스 제공</li>
                            <li>비자 상담 및 서류 작성 지원</li>
                            <li>법무부 출입국·외국인청 업무 대행</li>
                        </ul>
                        <p><strong>3. 개인정보의 보유 및 이용 기간</strong></p>
                        <ul>
                            <li>서비스 완료 후 5년간 보관 (관련 법령에 따라)</li>
                        </ul>
                    </div>

                    <div class="consent-checkbox" onclick="toggleCheckbox('consent2')">
                        <input type="checkbox" id="consent2" required>
                        <div class="consent-label">
                            <div class="title">
                                서비스 제공을 위한 정보 활용 동의
                                <span class="required">(필수)</span>
                                <span class="view-detail" onclick="event.stopPropagation(); viewDetail('consent2-detail')">자세히 보기</span>
                            </div>
                            <div class="desc">법무법인 로연이 출입국 민원 대행을 위해 귀하의 정보를 활용하는 것에 동의합니다.</div>
                        </div>
                    </div>

                    <div class="consent-box" id="consent2-detail" style="display: none;">
                        <h4>서비스 제공을 위한 정보 활용 동의</h4>
                        <p><strong>1. 정보 활용 목적</strong></p>
                        <ul>
                            <li>법무부 출입국·외국인청 민원 서류 작성</li>
                            <li>통합신청서 및 첨부 서류 준비</li>
                            <li>온라인/방문 신청 대행</li>
                            <li>진행 상황 안내 및 결과 통지</li>
                        </ul>
                        <p><strong>2. 정보 제공 대상</strong></p>
                        <ul>
                            <li>법무부 출입국·외국인청 (민원 처리 목적)</li>
                            <li>대한민국 정부 관련 기관 (법적 요구 시)</li>
                        </ul>
                    </div>

                    <div class="consent-checkbox" onclick="toggleCheckbox('consent3')">
                        <input type="checkbox" id="consent3" required>
                        <div class="consent-label">
                            <div class="title">
                                대학교-법무법인 협약에 따른 정보 공유 동의
                                <span class="required">(필수)</span>
                                <span class="view-detail" onclick="event.stopPropagation(); viewDetail('consent3-detail')">자세히 보기</span>
                            </div>
                            <div class="desc">소속 대학교와 법무법인 로연 간 서비스 제공을 위한 최소한의 정보 공유에 동의합니다.</div>
                        </div>
                    </div>

                    <div class="consent-box" id="consent3-detail" style="display: none;">
                        <h4>대학교-법무법인 협약에 따른 정보 공유</h4>
                        <p><strong>1. 정보 공유 목적</strong></p>
                        <ul>
                            <li>교육국제화역량 인증제 불법체류율 관리 지원</li>
                            <li>유학생 체류 관리 및 비자 전환 지원</li>
                            <li>서비스 이용 통계 제공 (개인 식별 불가 형태)</li>
                        </ul>
                        <p><strong>2. 공유되는 정보</strong></p>
                        <ul>
                            <li>학번, 국적, 현재 체류자격 (통계 목적)</li>
                            <li>서비스 이용 현황 (비식별 처리)</li>
                        </ul>
                    </div>
                </div>

                <div class="consent-section">
                    <h3><i class="fas fa-check-circle"></i> 선택 동의 항목</h3>
                    
                    <div class="consent-checkbox" onclick="toggleCheckbox('consent4')">
                        <input type="checkbox" id="consent4">
                        <div class="consent-label">
                            <div class="title">
                                서비스 안내 및 혜택 정보 수신 동의
                                <span class="optional">(선택)</span>
                            </div>
                            <div class="desc">비자 만료 알림, 새로운 서비스 안내, 특별 혜택 등의 정보를 이메일 또는 문자로 받는 것에 동의합니다.</div>
                        </div>
                    </div>
                </div>

                <div class="button-group">
                    <button type="button" class="btn btn-cancel" onclick="history.back()">
                        취소
                    </button>
                    <button type="submit" class="btn btn-submit" id="submitBtn" disabled>
                        동의하고 계속하기
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script src="js/ui-components.js"></script>
    <script>
        // URL 파라미터에서 서비스 정보 가져오기
        const urlParams = new URLSearchParams(window.location.search);
        const serviceId = urlParams.get('service');
        const serviceName = urlParams.get('name') || '출입국 민원 서비스';
        const serviceAmount = urlParams.get('amount') || '0';

        // 페이지 로드
        window.addEventListener('DOMContentLoaded', function() {
            const studentProfile = JSON.parse(localStorage.getItem('studentProfile'));
            if (!studentProfile) {
                // 서울대 전용 페이지 - 서울대 로그인으로 리다이렉트
                window.location.href = 'visa-login-snu.html';
                return;
            }

            // 서비스 정보 표시
            document.getElementById('serviceName').textContent = serviceName;
            document.getElementById('serviceAmount').textContent = parseInt(serviceAmount).toLocaleString() + '원';
            document.getElementById('applicantName').textContent = studentProfile.full_name;

            // 체크박스 변경 감지
            const checkboxes = document.querySelectorAll('input[type="checkbox"][required]');
            checkboxes.forEach(checkbox => {
                checkbox.addEventListener('change', validateForm);
            });
        });

        // 체크박스 토글
        function toggleCheckbox(id) {
            const checkbox = document.getElementById(id);
            checkbox.checked = !checkbox.checked;
            
            // 체크 시 시각적 효과
            const parent = checkbox.closest('.consent-checkbox');
            if (checkbox.checked) {
                parent.classList.add('checked');
            } else {
                parent.classList.remove('checked');
            }
            
            validateForm();
        }

        // 상세 내용 보기
        function viewDetail(detailId) {
            const detail = document.getElementById(detailId);
            if (detail.style.display === 'none') {
                detail.style.display = 'block';
            } else {
                detail.style.display = 'none';
            }
        }

        // 폼 검증
        function validateForm() {
            const requiredCheckboxes = document.querySelectorAll('input[type="checkbox"][required]');
            const allChecked = Array.from(requiredCheckboxes).every(cb => cb.checked);
            
            const submitBtn = document.getElementById('submitBtn');
            submitBtn.disabled = !allChecked;
        }

        // 폼 제출
        document.getElementById('consentForm').addEventListener('submit', function(event) {
            event.preventDefault();

            // 동의 정보 저장
            const consentData = {
                service_id: serviceId,
                service_name: serviceName,
                consent1: document.getElementById('consent1').checked,
                consent2: document.getElementById('consent2').checked,
                consent3: document.getElementById('consent3').checked,
                consent4: document.getElementById('consent4').checked,
                timestamp: new Date().toISOString()
            };

            localStorage.setItem('lastConsent', JSON.stringify(consentData));

            showToast('동의가 완료되었습니다. 서비스 신청 페이지로 이동합니다.', 'success', 2000);

            // 서비스 신청 페이지로 이동 (서울대 전용)
            setTimeout(() => {
                window.location.href = `visa-service-apply-snu.html?service=${serviceId}`;
            }, 2000);
        });
    </script>
</body>
</html>
